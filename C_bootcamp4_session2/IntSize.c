#include <stdlib.h>
#include <stdio.h>

int main()
{
    printf("An int is size: %ld\n", sizeof(int));
    printf("An int is size: %ld\n", sizeof(double));
    printf("An int is size: %ld\n", sizeof(char));
}