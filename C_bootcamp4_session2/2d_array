#include <stdlib.h>
#include <stdio.h>

int main(){
    int **array;
    int rows = 10;
    int columns = 20;
    array = malloc(rows * sizeof(int));
    for(int i = 0; i < rows; i++){
        array[i] = malloc(columns * sizeof(int));
    }
}